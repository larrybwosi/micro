name: Release

on:
  push:
    branches:
      - main
    tags:
      - 'v*'

jobs:
  build-windows-release:
    name: Build & Release Windows App (MSI)
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install Frontend Dependencies
        run: npm ci

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri -> target

      - name: Build Tauri App
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ startsWith(github.ref, 'refs/tags/v') && github.ref_name || format('v0.1.{0}', github.run_number) }}
          releaseName: ${{ startsWith(github.ref, 'refs/tags/v') && format('scryme micro {0}', github.ref_name) || format('scryme micro v0.1.{0}', github.run_number) }}
          releaseBody: 'Automated release for scryme micro'
          releaseDraft: false
          prerelease: false
